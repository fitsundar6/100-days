/**
 * ============================================================
 * ALPHA X GYM — DATABASE ADAPTER
 * (MIGRATED FROM SUPABASE TO POSTGRESQL + PRISMA ORM)
 * ============================================================
 * Supabase has been completely removed from this project.
 * All database operations are now securely handled by the
 * Node.js / TypeScript backend powered by Prisma ORM and PostgreSQL.
 *
 * REST Endpoints:
 * - Admin API:  /api/admin/*
 * - Public API: /api/public/*
 * - Auth API:   /api/auth/*
 */

console.log('✅ Alpha X Gym: Connected to PostgreSQL via Prisma ORM.');
